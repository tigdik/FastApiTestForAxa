fastapi==0.95.1
uvicorn==0.22.0
sqlalchemy==2.0.7
pydantic==1.10.7
pytest==7.3.1
requests==2.28.2    # used only if you want to test via a live server
httpx==1.0.0b0        # used only if you want to test via a live server
